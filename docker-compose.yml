# docker compose up -d
services:
  jekyll:
    image: jekyll/jekyll:3.8
    container_name: jekyll-blog
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
    command: jekyll serve --host 0.0.0.0 --force_polling
